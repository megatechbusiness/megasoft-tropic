@model Megasoft2.mtReqUserBranchCostCentre
@{
    ViewBag.Title = "Index";
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { @id = "SalesPricingImport", @enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    if (!Html.ViewData.ModelState.IsValid)
    {
        <div class="alert alert-danger fade in">
            @Html.ValidationSummary(true)
        </div>
    }

    <fieldset>

        <div class="bs-example">
            <div class="panel panel-primary">
                <!-- Default panel contents -->
                <div class="panel-heading" style="max-height: 52px;">
                    <div class="row">
                        &nbsp&nbsp
                        <div class="btn-group btn-breadcrumb">
                            <a href="@Url.Action("Home", "Home")" class="btn btn-default"><i class="glyphicon glyphicon-home"></i></a>
                            <a href="@Url.Action("Index", "mtRequisitionUserBranchCostCentre")" class="btn btn-default">User Branch Cost Centre</a>

                        </div>

                        <div class="col-lg-8 pull-right">
                            <div class="col-lg-7">

                            </div>
                            <div class="col-lg-3 text-right">
                            </div>
                            <div class="col-lg-2 text-right">

                                <button type="submit" class="btn btn-success btn-sm" id="btnSave">
                                    <i class="fa fa-save"> Save</i>
                                </button>
                            </div>
                            &nbsp; &nbsp;
                        </div>
                    </div>
                </div>



                <div class="panel-body">
                    <div class="col-lg-12">

                        <div class="form-horizontal row spacer">
                            <div class="col-md-12 col-xs-12">
                                <label class="col-sm-2 control-label input-sm-label" style="text-align:left;">Username</label>
                                <div class="col-sm-4">
                                    @if (Model != null)
                                    {
                                        @Html.TextBoxFor(model => model.Username, new { @class = "form-control input-sm", @readonly = "readonly" })
                                    }
                                    else
                                    {
                                        @Html.DropDownListFor(model => model.Username, (IEnumerable<SelectListItem>)ViewBag.UserList, new { @class = "form-control input-sm" })
                                    }
                                </div>

                                <label class="col-sm-2 control-label input-sm-label" style="text-align:left;">Branch</label>
                                <div class="col-sm-4">
                                    @if (Model != null)
                                    {
                                        @Html.TextBoxFor(model => model.Branch, new { @class = "form-control input-sm", @readonly = "readonly" })
                                    }
                                    else
                                    {
                                        @Html.DropDownListFor(model => model.Branch, (IEnumerable<SelectListItem>)ViewBag.ReqBranchList, new { @class = "form-control input-sm" })
                                    }
                                </div>
                            </div>
                        </div>

                        <div class="form-horizontal row spacer">
                            <div class="col-md-12 col-xs-12">
                                <label class="col-sm-2 control-label input-sm-label" style="text-align:left;">CostCentre</label>
                                <div class="col-sm-4">
                                    @if (Model != null)
                                    {
                                        @Html.TextBoxFor(model => model.CostCentre, new { @class = "form-control input-sm", @readonly = "readonly" })
                                    }
                                    else
                                    {
                                        @Html.DropDownListFor(model => model.CostCentre, (IEnumerable<SelectListItem>)ViewBag.CostCentreList, new { @class = "form-control input-sm" })
                                    }
                                </div>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>

    </fieldset>
}


@*//mtRequisitionUserBranchCostCentreController*@
<div class="panel panel-primary modal-body">
    <button type="button" class="close" data-dismiss="modal">&times;</button>
    <div class="panel-heading">
        <h4 class="panel-title">
            <a data-toggle="collapse" data-target="#collapseOne"
               href="#collapseOne" class="collapsed">
                Stock Code
            </a>
        </h4>
    </div>
    <div class="panel-body" id="gl">
        <div class="col-sm-12">
            <div class="form-group">
                @*<div class="form-horizontal row">
                        <div class="form-group">
                            <div class="editor-field input-sm col-sm-12">
                                <input type="text" id="txtStockCodeSearch" class="form-control input-sm" placeholder="Search" autofocus />
                            </div>
                        </div>
                    </div>*@
                <div class="form-horizontal row">
                    <div class="form-group">
                        <div id="StockCodeResults" class="table-responsive">
                            <table id="StockCodeSearch" class="table table-hover table-striped table-condensed table-responsive table-bordered">
                                <thead><tr><th>Stock Code</th><th>Description</th></tr></thead>
                                <tbody id="data"></tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


<link href="~/Content/DataTable/datatables.css" rel="stylesheet">
@Scripts.Render("~/Content/DataTable/datatables.js")
<script type="text/javascript">
    function GetStockCodes()
    {
        //$.getJSON('StereoSystemAddStereo/StockCodeList?FilterText=' + $('#txtStockCodeSearch').val(), function (data) {
        //    var items = '<table id="StockCodeSearch" class="table table-hover table-striped table-condensed table-responsive table-bordered"><tr><th>Stock Code</th><th>Description</th></tr>';
        //    $.each(data, function (i, x) {
        //        items += "<tr class='nr' id=" + x.StockCode + "  data-dismiss='modal'><td><a href=#>" + x.StockCode + "</a></td><td>" + x.Description + "</td></tr>";
        //    });
        //    items += "</table>";
        //    $('#StockCodeResults').html(items);
        //    //alert(items);
        //});

        var table = $("#StockCodeSearch tbody");

        $.ajax({
            url:"@Url.Action("StockCodeList","StereoSystemAddStereo",new { FilterText = " " })",
            method: "GET",
            xhrFields: {
                withCredentials: true
            },
            success: function (data) {
                table.empty();
                $.each(data, function (a, b) {
                    table.append("<tr class='nr' id=" + b.StockCode + "  data-dismiss='modal'><td><a href=#>" + b.StockCode + "</a></td>" +
                        "<td>" + b.Description + "</td></tr>");
                });

                $("#StockCodeSearch").DataTable();
            }

        });
    }

    $(document).ready(function () {
        GetStockCodes();
        // $('#txtStockCodeSearch').focus();
        // $('#txtStockCodeSearch').keyup(function () {
        //    var val = $.trim($(this).val()).replace(/ +/g, ' ').toLowerCase();
        //    var $rows = $('#StockCodeSearch .nr');
        //    $rows.show().filter(function () {
        //        var text = $(this).text().replace(/\s+/g, ' ').toLowerCase();
        //        return !~text.indexOf(val);
        //    }).hide();
        //});

         $('#StockCodeResults').on("click", ".nr", function () {
            var rCode = $(this).attr('id');
            //do something with id
             $('#HeaderStockCode').val(rCode);
            window.setTimeout(function () {
            }, 500);
        });
         //$('#txtStockCodeSearch').focus();

    });
</script>